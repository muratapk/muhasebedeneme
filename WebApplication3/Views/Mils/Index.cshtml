@model IEnumerable<WebApplication3.Models.Mil>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MilAdi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MilAdi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MilId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MilId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MilId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
