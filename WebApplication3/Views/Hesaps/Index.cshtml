@model IEnumerable<WebApplication3.Models.Hesap>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Maliyet_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Personel_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Adet)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Maliyet_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Personel_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adet)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Hesap_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Hesap_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Hesap_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
