@model IEnumerable<WebApplication3.Models.Personel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AdSoyad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TcNo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Iban)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Brans_Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AdSoyad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TcNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Iban)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Brans_Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Personel_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Personel_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Personel_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
